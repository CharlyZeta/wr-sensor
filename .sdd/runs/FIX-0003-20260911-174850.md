# EXECUTION AUDIT: FIX-0003 (ingestion-service — outbox + idempotencia)
# Started: 2026-09-11 17:48:50
# Protocol: protocol/loop.md (v0.3.0) | Stack: stack.md

## Gate (EXPRESS) — HO-Gate humano 2026-09-10
- Resoluciones: (1) clave idempotente = eventId si viene, si no (sensorId,timestamp) — autonomo, sin esperar backlog de schema; (2) publisher = poller reactivo configurable (sin CDC/Kafka); (3) alcance: solo ingestion (at-least-once declarado; dedupe de alerting = contrato futuro); (4) agotar reintentos -> FALLIDO + ultimo_error + log, nunca descartar; (5) retencion dedupe/outbox 90 dias, purgada por el poller.
- Status APPROVED / Mode LOOP. Completion Map 19 items (Main Flow + BR-001..010 + AC-001..008).

## COMPLETION REPORT: FIX-0003 (2026-09-11)
- Implementacion (ingestion-service): tablas lectura_procesada (PK clave) y outbox_alerta (BIGSERIAL, estado/intentos/ultimo_error) en TimescaleInitializer (bloqueante y tolerante a falta de extension); IngestaTransaccionalPort + R2dbcIngestaTransaccionalAdapter con TransactionalOperator (lectura+dedupe+outbox en UNA transaccion, ON CONFLICT DO NOTHING -> duplicado sin escribir); IngestorLecturas dejo de publicar (usa outbox); ClaveIdempotencia (eventId o natural sensorId+ts); AlertaEventoJson; OutboxAlertaPoller (claim por lote con FOR UPDATE SKIP LOCKED, orden explicito por id — RETURNING no garantiza orden, backoff configurable, FALLIDO+ultimo_error, purga por retencion); parser acepta eventId opcional (BR-010); eliminado publisher directo (RabbitAlertaPublisher + port) y config ingestion.outbox.*.
- Bugs encontrados y corregidos durante el Loop: (1) schema async causaba race con el arranque -> init bloqueante; (2) estado en memoria de severidad compartido entre tests -> limpieza por test; (3) UPDATE...RETURNING devolvia filas desordenadas -> sort por id antes de publicar (BR-006).
- Tests: IngestorLecturasTest 10 (FEAT-0011 + FIX-0003 BR-001/002/004/010, AC-001), SeveridadEvaluadorTest 3, FIX0003MainFlowIT 7 (AC-001 redelivery, AC-002 rollback, AC-003/004 outbox+poller sin reenvio, AC-005 recuperacion, AC-006 Rabbit caido→FALLIDO, AC-007 orden, AC-008 purga sin tocar hypertable), FEAT0011MainFlowIT 1 (regresion).
- ingestion-service: 13 unit + 8 ITs = 21 verdes. BUILD SUCCESS (Maven offline).
- Contract: Status RESOLVED, Completion Map 19/19 OK.
