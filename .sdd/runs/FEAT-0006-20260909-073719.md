# EXECUTION AUDIT: FEAT-0006 (POST /api/auth/login — JWT)
# Started: 2026-09-09 07:37:19
# Protocol: protocol/loop.md (v0.3.0) | Stack: stack.md

## Gate (EXPRESS) — HO-Gate humano 2026-09-09
- Resoluciones: (1) ITs existentes migran a JWT real via login (sin camino literal en prod); (2) seed 2 usuarios (admin@wrsensor.local ADMIN, viewer@wrsensor.local VIEWER, dev passwords Admin123!/Viewer123!); (3) response login {token, rol, expiraEnSegundos}; (4) expiracion 1h (3600s).
- Status APPROVED / Mode LOOP. Completion Map 21 items.

## Decision de implementacion JWT (orquestador)
jjwt no esta cacheado en .m2 (Maven offline). nimbus-jose-jwt: <por verificar>. Si no hay libreria offline completa, se implementa HS256 con JDK estandar (javax.crypto.Mac + Base64url + Jackson claims) sin dependencias nuevas — cumple BR-003 (HS256, claims sub/rol/iat/exp, secret configurable). Desviacion de las Notas de implementacion del Contract (indicativas, no generan AC): documentada aqui y en el reporte.

## COMPLETION REPORT: FEAT-0006 (2026-09-09)
- Implementacion: Usuario (domain), LoginUseCase/LoginService + ports (FindUserByEmailPort, PasswordVerifier, TokenIssuer, TokenVerifier), JwtAdapter (HS256 JDK, sin deps nuevas — jjwt/nimbus incompletos en .m2 offline; desviacion de Notas de implementacion documentada), BCryptPasswordVerifier, UsuarioPersistenceAdapter, RolFilter (JWT real), AuthController /api/auth/login, DevUserSeeder (admin/viewer dev), schema usuario, config auth.jwt.*. Fix durante loop: Mono.just(null)/defaultIfEmpty(null) NPE en RolFilter -> reescrito con Mono.empty para rol ausente.
- Migracion (decision HO-Gate, tests reales): FEAT-0001/0002/0003 ITs autentican via /api/auth/login (TestTokens.login); OTHER = token minted rol AUDITOR; literal 'Bearer ADMIN' ya no aceptado (AC-008).
- Tests: JwtAdapterTest 7/7, LoginServiceTest 4/4, BCryptPasswordVerifierTest 1/1, FEAT0006MainFlowIT 8/8 (login admin/viewer, 401 credenciales, 400 body, protegido con JWT, expirado/firma invalida 401, literal 401).
- Suite completa sensor-registry: 79 unit/assert + 17 ITs (FEAT0001 1, FEAT0002 1, FEAT0003 7, FEAT0006 8) = 96 verdes. BUILD SUCCESS.
- Contract: Status RESOLVED, Completion Map 21/21 OK.
